name: just_game_engine
description: "A 2D game engine for Flutter."
version: 1.2.1
homepage: https://engine.justunknown.com/
repository: https://github.com/just-unknown-dev/just-game-engine
issue_tracker: https://github.com/just-unknown-dev/just-game-engine/issues

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_soloud: ^3.5.1
  just_storage: ^1.0.0
  just_database: ^1.1.0
  just_signals: ^1.0.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
flutter:

