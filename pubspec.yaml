name: just_game_engine
description: "A feature-rich 2D game engine for Flutter with ECS, physics, audio, tilemaps, particles, animation, and input systems for building cross-platform games."
version: 1.4.2
homepage: https://engine.justunknown.com/
repository: https://github.com/just-unknown-dev/just-game-engine
issue_tracker: https://github.com/just-unknown-dev/just-game-engine/issues

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio_engine: ^1.0.0
  just_storage: ^1.1.2
  just_database: ^1.3.0
  just_signals: ^1.0.1
  just_tiled: ^0.2.0
  meta: ^1.9.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

